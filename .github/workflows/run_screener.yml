name: Daily Options Scan

on:
  schedule:
    # Runs at 20:30 UTC (Approx 30 mins after market close)
    - cron: '30 20 * * 1-5'
  workflow_dispatch:      # Allows you to manually trigger the scan

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas numpy scipy

      - name: Run Screener
        env:
          # These MUST match the Secret names in your GitHub Settings exactly
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          EMAIL_RECIPIENT: ${{ secrets.EMAIL_RECIPIENT }}
        run: python bull_put_screener.py
